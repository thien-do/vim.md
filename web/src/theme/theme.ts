import { Button } from "@moai/core";

// ButtonSize doesn't have 20 (yet)
Button.sizes.medium.iconSize = 20 as any;
Button.sizes.mediumIcon.iconSize = 20 as any;
Button.sizes.small.iconSize = 16;
Button.sizes.smallIcon.iconSize = 16;
